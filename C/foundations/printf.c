#include <stdio.h>
#include <stdlib.h>

/* Exploring different applications of the printf() function. */

int main()
{
    printf("%d", 500); //format specifier '%d' allows us to pass an integer as an argument to the function

    printf("%s", "Hello World!") //format specifier '%s' allows us to pass a character(s) as an argument


    return 0;
}